apiVersion: apps/v1
kind: Deployment
metadata:
  name: teste-completo
  namespace: teste-completo
  labels:
    app: teste-completo
    managed-by: backstage
spec:
  replicas: 2
  selector:
    matchLabels:
      app: teste-completo
  template:
    metadata:
      labels:
        app: teste-completo
    spec:
      containers:
      - name: teste-completo
        image: durellirsd/teste-completo:v0.0.0
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: LOG_LEVEL
          value: "info"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
